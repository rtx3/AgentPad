# 会强制接管所有手柄 需要一个实时开关用来控制手柄接管与否

# Retrying in 0s · attempt 6/10 有这一条目应该被划定为error状态
# ⏺ API Error: Request rejected (429) · Upstream rate limit exceeded, please retry later 被划定为query状态而不是error
#    cp -r apps/dashboard/src/pages apps/dashboard/src/components apps/dashboard/src/api.ts
   apps/dashboard/src/test /tmp/exray-dashboard-legacy-src/ 2>/dev/null; mkdir -p
   /tmp/exray-dashboard-legacy-src && cp -r apps/dashboard/src /tmp/exray-dashboard-legacy-src/ && ls
   /tmp/exray-dashboard-legacy-src/src
   Backup old dashboard src to /tmp before replacement

 Do you want to proceed?
 ❯ 1. Yes
   2. Yes, and don't ask again for similar commands in /Users/mac/Zero/Proj/Coding/exray
   3. No
 没有被算做query

 